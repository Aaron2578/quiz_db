{
  "name": "quiz-api-server",
  "version": "1.0.0",
  "description": "Mock API for quiz questions using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch question.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
