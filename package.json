{
  "name": "quiz-api-server",
  "version": "1.0.0",
  "description": "Mock API using json-server",
  "scripts": {
    "start": "npx json-server --host 0.0.0.0 --watch questions.json --port $PORT",
    "server": "npx json-server --host 0.0.0.0 --watch questions.json --port $PORT",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
